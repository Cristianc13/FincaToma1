@model IEnumerable<FincaToma1.Models.Ubicacion>

@{
    ViewData["Title"] = "Index";
}

<h1>Index</h1>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.NombreU)
            </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.NombreU)
            </td>
            <td>
                <a asp-action="Edit" asp-route-id="@item.IDU">Edit</a> |
                <a asp-action="Details" asp-route-id="@item.IDU">Details</a> |
                <a asp-action="Delete" asp-route-id="@item.IDU">Delete</a>
            </td>
        </tr>
}
    </tbody>
</table>
